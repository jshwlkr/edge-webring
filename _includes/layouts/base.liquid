<!doctype html >
<html lang="{{ ring.locale }}">
    {% include 'partials/head' %}
    <body class="{{ class | join: " " }}">
        {{ content }}
        {% include 'partials/footer' %}
    </body>
</html>