<h2>Members
    <span>({{ members.size }})</span>
</h2>
<ul>
    {% for member in members  %}
        {% if member[1].name and member[1].website %}
            <li>{% include 'partials/member-list-entry', member: member[1] %}</li>
        {% endif %}
    {% endfor  %}
</ul>